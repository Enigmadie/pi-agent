name: Deploy home-agent

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  deploy:
    runs-on:
      - self-hosted
      - home-agent

    steps:
      - name: Deploy
        run: /home/godo/deploy/home-agent.sh
