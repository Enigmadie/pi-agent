# Check Stack

Read-only checklist example:

1. Check Docker containers.
2. Check Compose stack status.
3. Check reverse proxy route with an HTTP request.
4. Check service health endpoint.
5. Summarize failures and ask before changing anything.

Expected:
- containers are running;
- health endpoints return success;
- reverse proxy route is reachable.
